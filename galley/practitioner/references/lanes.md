# Mechanical lane coverage and reader assignments

Read the approved PLAN.md and **reuse its exact approved config unchanged**.
Every lane below remains required when applicable under the approved scope;
poetry-touch's explicit locks take precedence. Efficiency changes context handling,
not the number of readers, their coverage, or their approved model/effort.

1. Wave 1 starts with the SIX-WINDOW chapter sweep: Luna (paid, enabled by
   mechanical-wave) PLUS Sonnet ($0 session subagents), with imported Sonnet
   findings accounted for before the ladder's own findings. Both families matter.
2. Run the full typed-pass Luna+Sonnet ensemble union with Luna verifier, all
   approved deterministic/bespoke sweeps, LT at approved posture, repair,
   low-confidence confirmation, and BOTH meaning/fix gates. `error_types` must
   remain populated or the ensemble is inert. Candidate screening uses the approved
   Luna judge and deployment apply guard; never bypass it or disable a gate to
   rescue a held finding. `storysheet` (Luna) and `variant: auto` preserve voice
   and the book's own English spelling.
3. Review the profile's complete number extraction: every numeral/spelled number,
   house style, arithmetic, dates/ages, counts, distances, times, and continuity.
   Contradictions needing author knowledge are questions, never fabricated facts.
4. Whole-book continuity (timeline/age/date/attribute/object drift plus the
   deterministic calendar check) and per-chapter physical continuity are $0 Opus
   subscription reads, query-only. Do not enable paid continuity config lanes as
   substitutes. Sonnet→Opus type-and-compare retypes paragraphs minimally and
   diffs against source; real-word suggestions still face all fact/meaning guards.
5. For the extra narrow judgment lane, read `references/judgment.md` and preserve
   its mandate. Fixed chapter windows, approximately 24k characters, carry only a
   one-page rolling casefile of whole-book facts. Use Fable when the thread truly
   spans the book. Require incremental findings written to files, not only chat.

Every planned $0 fleet uses fixed windows × TWO passes: mechanics, then a slow
omission/duplication/sense type-and-compare. Do not replace that second pass with
another redundant fleet, and do not drop planned lanes. Keep a reader/window
assignment record so Verify rotates readers away from their own edits. Every
manuscript paragraph must remain covered; short windows are a routing choice,
not permission to sample or skip.

Read only counts/summary/dollar lines from ladder logs; keep findings and coverage
in artifacts. Confirm `findings.checkpoint.json` exists before finish/build risk.
Screen the whole sentence with each imported change, then adjudicate once with
whole-book concordance and rebuild once from the accepted decisions; later
settlement rebuilds are engine-owned. Multiple manual rebuilds indicate a missing
adjudication step. Record EVERY numbered plan line via `galley plan-line` as
ran/skipped/deferred with evidence or reason; $0 is still a promised plan line.
